<h2>{{title}}</h2>
<div class="testimonials">
    {{#each testimonials}}
    <div class="testimonial-card">
        <p><strong>{{name}}</strong> - {{rating}} Stars</p>
        <p>{{comment}}</p>
        <p>{{date}}</p>
    </div>
    {{/each}}
</div>
